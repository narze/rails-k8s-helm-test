apiVersion: v1
kind: Service
metadata:
  name: {{ template "rails-k8s-test.fullname" . }}
  labels:
    app: {{ template "rails-k8s-test.name" . }}
    chart: {{ template "rails-k8s-test.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "rails-k8s-test.name" . }}
    release: {{ .Release.Name }}
